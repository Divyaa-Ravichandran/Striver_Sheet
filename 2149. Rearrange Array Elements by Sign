

URL:  https://leetcode.com/problems/rearrange-array-elements-by-sign/description/


class Solution:
    def rearrangeArray(self, nums: List[int]) -> List[int]:
        '''

        #TC: O(N+N/2) where O(N) for traversing and  O(N/2) #for adding those elements alternatively to the array, where N = size of the array.

        #SC: O(N/2+N/2) where each N/2 will be for the space required for pos and neg.

        pos=[]
        neg=[]
        for i in range(len(nums)):
            if nums[i]>0:
                pos.append(nums[i])
            else:
                neg.append(nums[i])

        for i in range(len(pos)):
          
            nums[2*i]=pos[i]
        for i in range(len(neg)):
            
            nums[2*i+1]=neg[i]


        return nums
        '''

OR


        #TC: O(N) for Traversing once and SC: O(N) for extra space 

        n=len(nums)
        ans=[0]*n

        pos,neg=0,1
        for i in range(n):
            if nums[i]<0:
                ans[neg]=nums[i]
                neg+=2
            else:
                ans[pos]=nums[i]
                pos+=2

        return ans

