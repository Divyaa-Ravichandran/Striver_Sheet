
URL:  https://leetcode.com/problems/powx-n/description/

class Solution:
    def myPow(self, x: float, n: int) -> float:
        
        if n<0:
            x=1/x
            n=abs(n)

        ans=1.0
        for i in range(n):
            ans=ans*x
       
        return ans
        




OR


class Solution:
    def myPow(self, x: float, n: int) -> float:
        
        return x**n
